<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hp.doctororiginal">


    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.SEND_SMS" />


    <application
        android:allowBackup="true"
        android:icon="@drawable/doc"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Doc">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name=".MyService" android:label="MyService" android:enabled="true">
        <!--<intent-filter>
            <action android:name="com.example.hp.doctororiginal.MyService"/>

        </intent-filter>-->
        </service>
        <activity android:name=".ButtonChoice" />
        <activity android:name=".DoctorDetail" />
        <activity android:name=".ClerkLogin" />
        <activity android:name=".DoctorLogin" />
        <activity android:name=".DoctorRegister" />
        <activity android:name=".DoctorChoice" />
        <activity android:name=".ClerkRegister" />
        <activity android:name=".PatientDetails" />
        <activity
            android:name=".ClerkChoice"
            android:label="@string/title_activity_clerk_choice" />
        <activity android:name=".ViewPatientDetail" />
        <activity android:name=".Message" />
        <activity android:name=".ViewDoctorDetail"></activity>
    </application>

</manifest>